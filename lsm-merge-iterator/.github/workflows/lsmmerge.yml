name: lsm-merge-iterator CI

on:
  push:
    paths:
      - "lsm-merge-iterator/**"
      - ".github/workflows/lsmmerge.yml"
  pull_request:
    paths:
      - "lsm-merge-iterator/**"

defaults:
  run:
    working-directory: lsm-merge-iterator

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install -e .[dev]
      - run: ruff check src tests
      - run: ruff format --check src tests
      - run: mypy --strict src
      - run: pytest -q
