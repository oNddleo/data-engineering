name: customer-lifetime-value-segmenter

on:
  push:
    branches: [main]
    paths: ["customer-lifetime-value-segmenter/**", ".github/workflows/clvseg.yml"]
  pull_request:
    paths: ["customer-lifetime-value-segmenter/**", ".github/workflows/clvseg.yml"]
  workflow_dispatch:

defaults:
  run:
    working-directory: customer-lifetime-value-segmenter

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - run: pip install --upgrade pip && pip install -e ".[dev]"
      - run: python -m ruff check src tests
      - run: python -m ruff format --check src tests
      - run: python -m mypy src
      - run: python -m pytest tests -q

  docker:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - run: docker build -t clvseg:ci customer-lifetime-value-segmenter
      - run: docker run --rm clvseg:ci info
