# Phase 3-4 — Rust-accelerated ingestion + transform
polars>=1.0,<2            # Rust engine: source read + all transforms
connectorx>=0.3.3         # fast Postgres -> Arrow reader used by Polars
pyiceberg[s3fs,pyarrow]>=0.8,<0.10   # Iceberg writes + REST catalog client
pyarrow>=15
fsspec>=2024.0
s3fs>=2024.0
pytest>=8                 # transform unit tests
