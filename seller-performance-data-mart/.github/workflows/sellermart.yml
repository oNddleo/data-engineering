name: seller-performance-data-mart

on:
  push:
    branches: [main]
    paths: ["seller-performance-data-mart/**", ".github/workflows/sellermart.yml"]
  pull_request:
    paths: ["seller-performance-data-mart/**", ".github/workflows/sellermart.yml"]
  workflow_dispatch:

defaults:
  run:
    working-directory: seller-performance-data-mart

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - run: pip install --upgrade pip && pip install -e ".[dev]"
      - run: python -m ruff check src tests
      - run: python -m ruff format --check src tests
      - run: python -m mypy src
      - run: python -m pytest tests -q

  docker:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - run: docker build -t sellermart:ci seller-performance-data-mart
      - run: docker run --rm sellermart:ci info
