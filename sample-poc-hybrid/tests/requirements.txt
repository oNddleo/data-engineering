# Test deps — host venv, no Docker required for unit/.
pytest==8.3.3
pyspark==4.0.0
delta-spark==4.0.0
chispa==0.10.1
pillow==10.4.0
piexif==1.1.3

# Integration smoke (slow) — guarded behind pytest marker `slow`.
testcontainers==4.8.2
boto3==1.34.162
confluent-kafka==2.5.3
