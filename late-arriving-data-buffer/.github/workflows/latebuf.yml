name: late-arriving-data-buffer

on:
  push:
    branches: [main]
    paths: ["late-arriving-data-buffer/**", ".github/workflows/latebuf.yml"]
  pull_request:
    paths: ["late-arriving-data-buffer/**", ".github/workflows/latebuf.yml"]
  workflow_dispatch:

defaults:
  run:
    working-directory: late-arriving-data-buffer

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - run: pip install --upgrade pip && pip install -e ".[dev]"
      - run: python -m ruff check src tests
      - run: python -m ruff format --check src tests
      - run: python -m mypy src
      - run: python -m pytest tests -q

  docker:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - run: docker build -t latebuf:ci late-arriving-data-buffer
      - run: docker run --rm latebuf:ci info
