# rate-limiter-toolkit

Rate limiter implementations:
- **Token Bucket**: smooth burst absorption, configurable capacity and refill rate.
- **Sliding Window Counter**: precise per-second request counting without burst spikes.

Both are deterministic (clock-injectable), JSONL-serialisable, and zero-dependency.

## License
MIT
